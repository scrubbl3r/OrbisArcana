# Enemies Content

Stable enemy archetype definitions live here.

This domain owns enemy identity, default tuning, personality ranges, spawn
profile defaults, and registry metadata. It should stay declarative enough for
runtime systems, level content, and authoring publish tools to consume.

Enemy placement and encounter timing belong to level content. Live simulation
belongs to `src/game-runtime/enemies/`.

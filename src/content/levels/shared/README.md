Shared level-authored assets live here.

This directory is for content reused across multiple authored levels, such as:
- shared SVG conventions or starter templates
- palette/material references
- reusable marker/annotation conventions
- future cross-level element presets

Level-unique authored assets should stay in their own `level-*` package.

Staging hosts non-production runtime environments.

Use this area for:
- composite staging shells that compose multiple staging surfaces
- developer/operator tooling used to inspect runtime behavior
- playable runtime harnesses used to exercise game systems safely

Subdirectories here should host staging environments, not own the reusable game
runtime itself.

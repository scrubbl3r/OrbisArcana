export const RECEIVER_CONFIG_DEFAULT = Object.freeze({
  liftMixer: Object.freeze({
    weights: Object.freeze({
      groove: 0.35, // 25
      smooth: 0.25, // 35
      speed: 0.45, //40
    }),
  }),
});

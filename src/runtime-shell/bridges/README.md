Shell bridge code lives here.

Use this for composition bridges that connect authored/runtime actions to concrete shell hooks,
without turning `game-runtime/` or `runtime-effects/` into wiring layers.
